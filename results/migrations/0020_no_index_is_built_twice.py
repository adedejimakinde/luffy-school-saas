"""Drop five foreign-key indexes that a unique constraint already covers.

Every one of the five frozen tables hanging off `ReleasedCard` is unique on a
key **leading with `card_id`**, so Django's automatic index on that foreign key
is a second btree over the same answer: same column order, same rows, paid for
on every insert. `freeze_for_release()` bulk-inserts about five hundred rows per
class per release, and each index is paid for on every one of them.

Three of the five were re-keyed onto the card by `0019` and
`NoIndexIsBuiltTwiceTests` caught them immediately. The other two —
`ReleasedSubjectResult` and `ReleasedAssessmentScore` — have carried the
redundancy since `0016`, and were invisible only because that test named the two
trait tables and no others. It names all five now.

Nothing loses an index it needs. Both reads this one would serve — every frozen
row of one card, and the `PROTECT` check asking whether any child references a
card about to be deleted — are `WHERE card_id = …`, which the unique
constraint's leading column answers.
"""

# Generated by Django 5.2.17 on 2026-08-31 00:17

import django.db.models.deletion
from django.db import migrations, models


class Migration(migrations.Migration):

    dependencies = [
        ("results", "0019_a_card_can_be_reissued"),
    ]

    operations = [
        migrations.AlterField(
            model_name="releasedassessmentscore",
            name="card",
            field=models.ForeignKey(
                db_index=False,
                on_delete=django.db.models.deletion.PROTECT,
                related_name="assessment_scores",
                to="results.releasedcard",
            ),
        ),
        migrations.AlterField(
            model_name="releasedcomment",
            name="card",
            field=models.ForeignKey(
                db_index=False,
                on_delete=django.db.models.deletion.PROTECT,
                related_name="comments",
                to="results.releasedcard",
            ),
        ),
        migrations.AlterField(
            model_name="releasedsessionresult",
            name="card",
            field=models.ForeignKey(
                db_index=False,
                on_delete=django.db.models.deletion.PROTECT,
                related_name="session_results",
                to="results.releasedcard",
            ),
        ),
        migrations.AlterField(
            model_name="releasedsubjectresult",
            name="card",
            field=models.ForeignKey(
                db_index=False,
                on_delete=django.db.models.deletion.PROTECT,
                related_name="subject_results",
                to="results.releasedcard",
            ),
        ),
        migrations.AlterField(
            model_name="releasedtraitrating",
            name="card",
            field=models.ForeignKey(
                db_index=False,
                on_delete=django.db.models.deletion.PROTECT,
                related_name="trait_ratings",
                to="results.releasedcard",
            ),
        ),
    ]
